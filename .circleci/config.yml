version: 2.1

jobs:
  check_cpu:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Check Number of CPUs
          command: |
            echo "Checking number of CPUs:"
            lscpu

workflows:
  version: 2
  check:
    jobs:
      - check_cpu
